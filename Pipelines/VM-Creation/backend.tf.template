# Recommended backend.tf for remote state management

terraform {
  backend "azurerm" {
    # Values will be provided via pipeline
    # storage_account_name = "from_pipeline"
    # container_name       = "from_pipeline" 
    # key                  = "terraform.tfstate"
    # resource_group_name  = "from_pipeline"
  }
}