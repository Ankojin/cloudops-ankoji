trigger:
- none  # Manual trigger only

pool:
  vmImage: 'ubuntu-latest'

parameters:
  - name: action
    displayName: "Action to Perform"
    type: string
    default: "apply"
    values:
      - apply
      - modify
      - destroy
  - name: project
    displayName: "Project Name"
    type: string
    default: "default-project"
  - name: useDestroyTargets
    displayName: "Use Destroy Targets"
    type: boolean
    default: false
  - name: destroyTargets
    displayName: "Destroy Targets"
    type: string
    default: ""  # Comma-separated list of resource targets for destroy

variables:
  - name: tfWorkingDirectory
    value: "$(Build.SourcesDirectory)/On-Prem-VM-Migration/Testing"
  - group: TerraformVariables
  - name: pythonVersion
    value: "3.9"

steps:
# Step 1: Set Environment Variables
- script: |
    echo "Setting environment variables..."
    export PROJECT_NAME="${{ parameters.project }}"
    echo "##vso[task.setvariable variable=PROJECT_NAME]$PROJECT_NAME"
    export STORAGE_CONTAINER_NAME="${PROJECT_NAME}-tfstatefs"
    echo "##vso[task.setvariable variable=STORAGE_CONTAINER_NAME]$STORAGE_CONTAINER_NAME"
    echo "Using Project: $PROJECT_NAME"
    echo "Using Storage Container: $STORAGE_CONTAINER_NAME"
  displayName: "Set Environment Variables"

# Step 2: Azure Login
- script: |
    echo "Logging into Azure..."
    az login --service-principal \
        -u $(ARM_CLIENT_ID) \
        -p $(ARM_CLIENT_SECRET) \
        --tenant $(ARM_TENANT_ID) || { echo "Azure login failed! Exiting."; exit 1; }
    echo "Azure login successful."
  displayName: "Azure Login"

# Step 3: Create or Verify Azure Storage Container
- script: |
    echo "Verifying or creating storage container..."
    containerExists=$(az storage container exists \
        --name $(STORAGE_CONTAINER_NAME) \
        --account-name $(storageAccountName) \
        --account-key $(storageAccountKey) \
        --query 'exists' -o tsv) || { echo "Error checking container existence."; exit 1; }

    if [ "$containerExists" == "true" ]; then
        echo "Storage container exists: $STORAGE_CONTAINER_NAME"
    else
        echo "Creating storage container: $STORAGE_CONTAINER_NAME"
        az storage container create \
            --name $(STORAGE_CONTAINER_NAME) \
            --account-name $(storageAccountName) \
            --account-key $(storageAccountKey) || { echo "Failed to create storage container."; exit 1; }
    fi
  displayName: "Verify/Create Storage Container"
  condition: eq('${{ parameters.action }}', 'apply')

# Step 4: Validate Terraform Configuration
- script: |
    echo "Validating Terraform configuration..."
    cd $(tfWorkingDirectory)
    terraform init -backend=false || { echo "Terraform initialization failed! Exiting."; exit 1; }
    terraform validate || { echo "Terraform validation failed! Exiting."; exit 1; }
    echo "Terraform configuration is valid."
  displayName: "Validate Terraform Configuration"

# Step 5: Parallel File Upload/Download Preparation
- task: Bash@3
  displayName: "Parallel File Upload/Download Preparation"
  condition: or(eq('${{ parameters.action }}', 'modify'), eq('${{ parameters.action }}', 'destroy'))
  inputs:
    targetType: 'inline'
    script: |
      echo "Executing parallel file upload/download preparation."
      # Use & to run processes in the background
      (
          echo "Downloading state files..."
          az storage blob download-batch \
            --account-name $(storageAccountName) \
            --destination $(tfWorkingDirectory) \
            --source $(STORAGE_CONTAINER_NAME) \
            --pattern "*.tfstate*" \
            --account-key $(storageAccountKey) || echo "Error downloading state files."
      ) &

      (
          echo "Downloading lock files..."
          az storage blob download-batch \
            --account-name $(storageAccountName) \
            --destination $(tfWorkingDirectory) \
            --source $(STORAGE_CONTAINER_NAME) \
            --pattern "*.lock.*" \
            --account-key $(storageAccountKey) || echo "Error downloading lock files."
      ) &

      wait  # Wait for all background jobs to finish
      echo "File preparation completed."

# Step 6: Terraform Apply/Modify Logic
- script: |
    echo "Executing Terraform ${action}..."
    cd $(tfWorkingDirectory)
    terraform init || { echo "Terraform initialization failed! Exiting."; exit 1; }
    terraform plan -out=tfplan || { echo "Terraform plan failed! Exiting."; exit 1; }
    terraform apply -auto-approve tfplan || { echo "Terraform apply failed! Exiting."; exit 1; }
    echo "Terraform ${action} completed successfully."
  displayName: "Terraform Apply/Modify Logic"
  condition: or(eq('${{ parameters.action }}', 'apply'), eq('${{ parameters.action }}', 'modify'))

# Step 7: Upload Terraform Files (Parallel)
- task: Bash@3
  displayName: "Upload Terraform Files (Parallel)"
  condition: or(eq('${{ parameters.action }}', 'apply'), eq('${{ parameters.action }}', 'modify'))
  inputs:
    targetType: 'inline'
    script: |
      echo "Uploading files in parallel..."
      (
          az storage blob upload-batch \
            --account-name $(storageAccountName) \
            --destination $(STORAGE_CONTAINER_NAME) \
            --source $(tfWorkingDirectory) \
            --pattern "*.tfstate*" \
            --account-key $(storageAccountKey) \
            --overwrite true || echo "Error uploading state files."
      ) &

      (
          az storage blob upload-batch \
            --account-name $(storageAccountName) \
            --destination $(STORAGE_CONTAINER_NAME) \
            --source $(tfWorkingDirectory) \
            --pattern "*.lock.*" \
            --account-key $(storageAccountKey) \
            --overwrite true || echo "Error uploading lock files."
      ) &

      wait
      echo "All files uploaded successfully."

# Step 8: Terraform Destroy Logic (with destroyTargets handling)
- script: |
    echo "Executing Terraform destroy..."
    cd $(tfWorkingDirectory)
    terraform init || { echo "Terraform initialization failed! Exiting."; exit 1; }
    if [ "${{ parameters.useDestroyTargets }}" == "true" ]; then
        echo "Destroying specific resources: ${{ parameters.destroyTargets }}"
        terraform destroy -auto-approve -target=$(echo ${{ parameters.destroyTargets }} | tr ',' '\n') || { echo "Terraform destroy failed! Exiting."; exit 1; }
    else
        echo "Destroying all resources..."
        terraform destroy -auto-approve || { echo "Terraform destroy failed! Exiting."; exit 1; }
    fi
    echo "Terraform destroy completed successfully."
  displayName: "Terraform Destroy Logic"
  condition: eq('${{ parameters.action }}', 'destroy')